media query:
o mesmo site em varias midias
usar pasta de css´s

media queries = media type(tipo de midia) + media features(característica de midia).
media features são indicados entre parenteses
$: orientation;
podemos também declarar tudo em uma unica css que usa o @media junto com os parametros citados acima
